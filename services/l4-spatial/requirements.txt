# CTT Layer 4 Spatial Dependencies
# Phase 14a: SUMO Spatial + OSM Network Generation
# Note: libsumo is installed via pip (has arm64 wheels) not apt libsumo-dev

# SUMO Python bindings (C++ API via pip wheel)
libsumo>=1.22.0

# Messaging
pyzmq>=25.0
kafka-python>=2.0

# Math & geometry
numpy>=1.24
shapely>=2.0

# OSM network extraction (optional — for osm_importer.py)
osmnx>=1.6
